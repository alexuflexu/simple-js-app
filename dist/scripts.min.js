let pokemonRepository = (function () {
  let t = [];
  function e(t) {
    return fetch(t.detailsUrl)
      .then(function (t) {
        return t.json();
      })
      .then(function (e) {
        (t.imageUrl = e.sprites.front_default),
          (t.height = e.height),
          (t.types = e.types);
      })
      .catch(function (t) {
        console.error(t);
      });
  }
  function n(t) {
    e(t).then(function () {
      let e = document.querySelector('.modal-body');
      e.innerHTML = '';
      let n = document.createElement('p');
      n.textContent = 'Height: ' + t.height;
      let o = document.createElement('img');
      (o.src = t.imageUrl),
        (o.alt = t.name),
        e.appendChild(n),
        e.appendChild(o);
      document.querySelector('#pokemonModalLabel').textContent = t.name;
      let i = document.querySelector('#pokemonModal');
      i.classList.add('show'),
        (i.style.display = 'block'),
        i.setAttribute('aria-modal', 'true'),
        i.setAttribute('role', 'dialog');
    });
  }
  return {
    getAll: function () {
      return t;
    },
    add: function (e) {
      t.push(e);
    },
    addListItem: function t(e) {
      let o = document.querySelector('.pokemon-list'),
        i = document.createElement('li');
      i.classList.add('list-group-item');
      let l = document.createElement('button');
      (l.innerText = e.name),
        l.classList.add('custom-button', 'btn', 'btn-primary'),
        l.setAttribute('data-toggle', 'modal'),
        l.setAttribute('data-target', '#pokemonModal'),
        i.appendChild(l),
        o.appendChild(i),
        l.addEventListener('click', function (t) {
          n(e);
        });
    },
    loadList: function e() {
      return fetch('https://pokeapi.co/api/v2/pokemon/?limit=150')
        .then(function (t) {
          return t.json();
        })
        .then(function (e) {
          e.results.forEach(function (e) {
            var n;
            let o = { name: e.name, detailsUrl: e.url };
            'object' == typeof (n = o) && 'name' in n
              ? t.push(n)
              : console.log('Error'),
              console.log(o);
          });
        })
        .catch(function (t) {
          console.error(t);
        });
    },
    loadDetails: e,
    showDetails: n,
  };
})();
pokemonRepository.loadList().then(function () {
  pokemonRepository.getAll().forEach(function (t) {
    pokemonRepository.addListItem(t);
  });
});
